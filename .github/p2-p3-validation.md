# P2/P3 validation

This branch exists only to run the pull-request CI workflow against the current P0–P3 Moment Engine implementation after the integration fixes discovered by the first validation run.
