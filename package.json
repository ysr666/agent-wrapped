{
  "name": "agent-wrapped",
  "version": "0.0.0",
  "private": true,
  "description": "The funniest moments, catchphrases and plot twists from your AI coding sessions.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
